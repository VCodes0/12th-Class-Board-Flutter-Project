import 'package:flutter/material.dart';

class JeeAdvanced extends StatelessWidget {
  const JeeAdvanced({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("JeeAdvanced"),
      ),
    );
  }
}
