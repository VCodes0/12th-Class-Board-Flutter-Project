import 'package:flutter/material.dart';

class Neet extends StatelessWidget {
  const Neet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("NEET")));
  }
}
